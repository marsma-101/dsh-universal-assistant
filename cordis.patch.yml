# Bundle patch for dsh-universal-assistant: activates the plugin row that registers the
# Agent Studio surface (sidebar entry + settings page). Plain insert only, so the
# host can hot-mount this package.
- insert:
    - id: universal-assistant
      name: dsh-universal-assistant
